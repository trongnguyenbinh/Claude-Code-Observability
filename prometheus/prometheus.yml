global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  # Scrape the Claude Code metrics that the OTel Collector exposes on port 8889.
  - job_name: otel-collector
    static_configs:
      - targets: ['otel-collector:8889']
